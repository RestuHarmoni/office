# Auto Task Generator Rules

Input: projectType + client requirement.
Output: department tasks with title, detail, department, priority, SOP checklist and QA checklist.

Default routing: PM → UIUX → Content → Frontend → Backend → SEO → QA → Client Management → Archive.
